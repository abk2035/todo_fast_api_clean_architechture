[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["app"]

[project]
name = "todo-fastapi"
version = "0.1.0"
description = "API Todo avec FastAPI"
authors = [{ name="Abk", email="elbobokabo@email.com" }]